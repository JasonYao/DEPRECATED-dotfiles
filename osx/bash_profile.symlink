# OSX specific dotfiles (alias to .bashrc)

if [ -n "$BASH_VERSION" ] && [ -f $HOME/.bashrc ];then
	source $HOME/.bashrc
fi

test -e "${HOME}/.iterm2_shell_integration.bash" && source "${HOME}/.iterm2_shell_integration.bash"
